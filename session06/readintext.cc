#include <iostream>
#include <string>
#include <sstream>
using namespace std;

int main() {
	string line = cin.getline();
	istringstream s(line);
	string cmd;
	s >> cmd;
	if (cmd == "ADD_FRONT") {

	}
}
